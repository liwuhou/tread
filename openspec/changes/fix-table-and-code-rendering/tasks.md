## 1. 测试（TDD - 先写测试）

- [ ] 1.1 在 `src/xhtml.rs` 的 `tests` 模块中添加表格解析测试：验证简单表格的解析
- [ ] 1.2 添加测试：验证带表头的表格解析
- [ ] 1.3 添加测试：验证表格渲染为 Unicode 边框格式
- [ ] 1.4 添加测试：验证代码块保留缩进和换行
- [ ] 1.5 添加测试：验证 HTML 实体字符的正确转换

## 2. 表格渲染实现

- [x] 2.1 定义表格数据结构：`struct Table { rows: Vec<Vec<String>>, has_header: bool }`
- [x] 2.2 在 `xhtml_to_lines` 函数中添加表格状态跟踪：`in_table`、`current_row`、`table_data`
- [x] 2.3 实现表格标签解析：table、thead、tbody、tr、th、td
- [x] 2.4 实现表格数据收集逻辑：在 tr 开始时创建新行，在 td/th 中收集文本
- [x] 2.5 实现表格渲染函数：将表格数据转换为 Unicode 边框格式的文本行
- [x] 2.6 实现列宽计算逻辑：遍历所有行，计算每列的最大宽度
- [x] 2.7 实现表格边框绘制：使用 Unicode 字符（┌─┬┐│├┼┤└─┴┘）
- [x] 2.8 添加表头样式：为表头行应用粗体样式

## 3. 代码块增强实现

- [ ] 3.1 检查现有 `in_pre` 模式的处理逻辑，确保不 trim 文本
- [ ] 3.2 验证 `<pre><code>` 嵌套结构的正确处理
- [ ] 3.3 确保代码块前后的空行分隔
- [ ] 3.4 验证 HTML 实体（&lt; &gt; &amp;）的正确转换

## 4. 宽表格处理

- [ ] 4.1 实现终端宽度检测（使用 `crossterm::terminal::size()` 或固定 80 列）
- [ ] 4.2 实现列宽压缩逻辑：当表格总宽度超过终端宽度时，按比例压缩
- [ ] 4.3 实现内容截断逻辑：当单元格内容超过列宽时，截断并添加 "..."

## 5. 手动验证

- [ ] 5.1 使用 `-i` 模式访问包含表格的网页（如 Wikipedia、技术文档），验证表格正确渲染
- [ ] 5.2 验证代码块保留缩进和格式
- [ ] 5.3 验证宽表格的压缩或截断行为
- [ ] 5.4 验证嵌套标签（如表格中的链接、粗体）被正确处理

## 6. 文档和提交

- [ ] 6.1 更新函数注释，说明表格和代码块的渲染逻辑
- [ ] 6.2 使用 Conventional Commits 格式提交：`feat(xhtml): add table rendering and enhance code block formatting`
